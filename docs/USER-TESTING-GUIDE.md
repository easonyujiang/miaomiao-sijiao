# 妙喵私教 — 真实环境搭建与用户测试手册

本文档面向**真实用户视角**的测试人员，所有操作通过浏览器完成，不涉及命令行测试工具。

---

## 一、环境搭建（一次性操作）

### 1.1 前置条件

| 依赖 | 版本要求 | 检查方式 |
|------|----------|----------|
| Python | >= 3.11 | 终端输入 `python --version` |
| Node.js | >= 18 | 终端输入 `node --version` |
| 任一 LLM API Key | Kimi 或 DeepSeek | 从对应平台获取 |

### 1.2 后端启动

```powershell
# 进入服务端目录
cd E:\Create\Code\ewa\server

# 创建虚拟环境并安装依赖（首次）
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -e ".[dev]"

# 配置环境变量（首次）
Copy-Item .env.example .env
# 用记事本打开 .env，填入至少一个 API Key：
#   MOONSHOT_API_KEY=sk-你的真实key
#   DEEPSEEK_API_KEY=sk-你的真实key（可选）

# 启动后端
python run.py
```

看到以下输出说明启动成功：
```
妙喵私教 starting up...
妙喵私教 ready. DB: E:\Create\Code\ewa\server\data\miaomiao.db
```

后端地址：**http://localhost:8000**（本地开发）或 **http://8.130.190.169:8000**（远程服务器）

> 当前插件默认已指向远程服务器 `http://8.130.190.169:8000`。如在本机启动后端，请修改 `extension/content/bilibili.js`、`extension/content/douyin.js` 和 `extension/content/voice.js` 中的 `API_BASE`，并在 `manifest.json` 的 `host_permissions` 中添加 `http://localhost:8000/*`。

### 1.3 前端启动（可选，用于开发模式）

```powershell
# 新开一个终端
cd E:\Create\Code\ewa\server\frontend
npm install
npm run dev
```

前端地址：**http://localhost:3000**（自动代理 API 请求到后端）

### 1.4 确认服务正常

打开浏览器访问 http://localhost:8000/health ，应看到：
```json
{"status":"ok","site":{"seeded":true},"timestamp":"2026-07-18T..."}
```

如果 `seeded` 为 `true`，说明种子数据已加载完毕。

---

## 二、测试场景总览

| 编号 | 场景 | 入口地址 | 优先级 |
|------|------|----------|--------|
| T1 | 博主个人网站首页浏览 | http://localhost:3000 | P0 |
| T2 | 妙喵聊天助手对话 | 网页右下角猫咪按钮 | P0 |
| T3 | 日记时间线浏览 | http://localhost:3000/diary | P1 |
| T4 | 项目展示页 | http://localhost:3000/projects | P1 |
| T5 | 管理后台 CRUD | http://localhost:8000/admin | P1 |
| T6 | Chrome 插件视频问答 | B站视频页 | P0 |
| T7 | 课程闯关答题 | 插件面板 / B站视频页 | P0 |
| T8 | 社区板块 | http://localhost:3000/community | P2 |
| T9 | 简历打印页 | http://localhost:3000/resume | P2 |

---

## 三、详细测试步骤

### T1 — 博主个人网站首页

**目标**：验证网站能正确展示博主信息、项目、视频等。

**操作步骤**：

1. 打开浏览器，访问 http://localhost:3000（开发模式）或 http://localhost:8000（生产模式）
2. 观察页面是否加载出博主头像、姓名「钟笑咪」、个人简介
3. 滚动页面，检查以下区块是否存在且有内容：
   - 「关于我」/ 个人简介区
   - 项目列表（应看到「妙喵 Creator Agent」「AI Tutorial Demo」等项目卡片）
   - 视频列表（应看到至少 2 个视频条目）
   - 日记预览区（应看到最近几条日记标题和日期）
   - FAQ / 常见问题区
   - 联系方式（邮箱、GitHub 链接）
4. 点击导航栏中的各链接（项目、日记、博客等），确认页面跳转正常

**预期结果**：
- 所有区块正确渲染，无空白或报错
- 项目卡片有标题、阶段、标签
- 点击视频能跳转到对应平台
- 页面整体风格一致，无明显排版错乱

---

### T2 — 妙喵聊天助手对话

**目标**：验证聊天机器人能正常响应，包括 FAQ 匹配、日记查询、LLM 改写。

**操作步骤**：

1. 打开网站首页，在**右下角**找到猫咪浮动按钮（🐾图标）
2. 点击按钮，弹出聊天窗口，妙喵应显示欢迎语：「你好，我是妙喵...」
3. **测试 FAQ 问答** — 依次输入以下问题，观察回答：

| 输入内容 | 预期回答方向 |
|----------|-------------|
| `你是谁` | 介绍自己是博主的数字分身 |
| `博主是谁` / `谁是钟笑咪` | 介绍钟笑咪的身份 |
| `有什么项目` | 列举已完成的项目 |
| `怎么联系` | 提供邮箱等联系方式 |
| `最近在做什么` | 提及最近的日记内容 |

4. **测试日记查询** — 输入：
   - `最近在忙什么`
   - `今天做了什么`
   - `日记`
   
   预期：妙喵应提及最近的日记条目（如 6 月底的日记）

5. **测试 LLM 风格改写** — 输入一个开放性问题：
   - `你对 AI 有什么看法`
   
   观察回答是否带有博主的个人风格（而非纯模板回复）

6. **测试视频上下文** — 如果页面上有视频，在聊天时带上视频上下文：
   - 输入 `这个视频讲了什么`
   
   观察是否引用了视频字幕内容

7. **测试离线回退** — 如果未配置 LLM Key：
   - 断开网络或使用无效 Key
   - 输入问题，观察是否回退到本地 FAQ 匹配
   - 回答应以「猫猫暂时连不上后端大脑」开头

**预期结果**：
- 每次对话都有回复（不超时）
- FAQ 类问题匹配准确
- 日记查询能返回最近日记内容
- LLM 回复自然，不是生硬的模板
- 无 500 错误或空白回复

---

### T3 — 日记时间线

**目标**：验证日记页面能正确展示日记列表。

**操作步骤**：

1. 访问 http://localhost:3000/diary
2. 检查页面是否展示日记时间线
3. 预期看到至少 6 条日记（种子数据），日期范围 2026-06-01 至 2026-06-29
4. 点击某条日记，查看详情（正文、心情、天气、地点、标签）
5. 检查日记正文中的 Markdown 格式是否正确渲染

**预期结果**：
- 日记按时间倒序排列
- 每条日记显示日期、标题、摘要
- 详情页正文完整，Markdown 格式正确（标题、列表等）

---

### T4 — 项目展示页

**目标**：验证项目列表和详情展示。

**操作步骤**：

1. 访问 http://localhost:3000/projects
2. 检查项目列表，预期看到 11 个种子项目
3. 确认以下项目存在且标记为「精选」：
   - 妙喵 Creator Agent
   - AI Tutorial Demo
   - Cline Memory Hub
   - 《献书》Interactive Narrative Game
4. 点击某个项目卡片，查看详情

**预期结果**：
- 项目列表完整，标签和阶段信息正确
- 精选项目有特殊标记
- 项目详情可展开查看

---

### T5 — 管理后台 CRUD

**目标**：验证管理后台的增删改查功能。

**操作步骤**：

1. 打开 http://localhost:8000/admin
2. **登录**：系统会要求输入 Token
   - 如果 `.env` 中设置了 `ADMIN_TOKEN`，使用该值
   - 如果未设置，查看后端启动日志中会打印随机生成的 Token
3. 登录后进入管理面板首页，应看到数据库各表的统计信息
4. **测试读取** — 点击左侧菜单中的表名（如 `profiles`、`faqs`、`videos`）：
   - 确认列表正确显示种子数据
   - 测试搜索功能（在搜索框输入关键词）
   - 测试分页（如果有）
5. **测试新增** — 选择一个可写的表（如 `faqs`），点击「新增」：
   - 填写表单字段
   - 提交后确认列表中出现新记录
6. **测试编辑** — 点击某条记录的「编辑」按钮：
   - 修改某个字段
   - 保存后确认修改生效
7. **测试删除** — 点击某条记录的「删除」按钮：
   - 确认弹出确认提示
   - 确认后记录从列表消失
8. **测试审计日志** — 点击「日志」菜单，确认前面的操作（新增/编辑/删除）都有日志记录

**预期结果**：
- 表列表正确显示各表行数
- CRUD 操作均正常工作
- 审计日志记录完整（包含操作类型、时间、IP）
- 无 500 错误

---

### T6 — Chrome 插件视频问答

**目标**：验证 Chrome 插件能在 B 站视频页注入猫咪私教，并支持语音输入。

**前置操作（首次）**：
1. 打开 Chrome，访问 `chrome://extensions/`
2. 打开右上角「开发者模式」开关
3. 点击「加载已解压的扩展程序」
4. 选择 `E:\Create\Code\ewa\extension` 目录
5. 确认「妙喵私教」扩展出现在列表中

**测试步骤**：

1. 打开 B 站视频：`https://www.bilibili.com/video/BV1mJ4m147PG`
2. 等待页面加载完成，视频右下角会出现妙喵猫咪气泡
3. 点击气泡展开面板，检查是否显示：
   - 视频标题
   - 状态徽章「练习模式」
   - 课程总关卡数（5 关）
4. **注册视频**：插件会自动向后端注册当前视频，检查面板是否显示视频标题和字幕数量
5. **视频问答**：
   - 在视频播放到某个时间点时（如 0:30），在插件输入框中提问
   - 输入 `什么是正当防卫`
   - 观察回答是否包含视频字幕上下文
   - 回答末尾是否有 `[SEEK:秒数]` 跳转提示
6. **语音输入**：
   - 按住输入框右侧的 🎤 按钮
   - 说出 `什么是正当防卫`
   - 松开后应显示「识别中…」，然后自动发送识别文本
   - 观察妙喵是否能基于语音识别结果作答
7. **点击跳转**：如果回答中有跳转建议，点击后视频应跳转到对应时间点
8. **LLM 不可用时**：
   - 如果未配置 API Key，回答应来自字幕搜索或本地 FAQ
   - 回答应标注「offline: true」

**预期结果**：
- 插件面板正确注入到 B 站页面
- 能识别当前视频并加载字幕/课程
- 文字问答有视频上下文相关性
- 语音输入能识别中文并自动发送
- 跳转功能正常
- 不影响 B 站页面本身的正常使用

---

### T7 — 课程闯关答题

**目标**：验证课程加载、答题评分、进度持久化的完整流程。

**前置**：需要先完成 T6（确保视频已注册）。

**测试步骤**：

1. **加载课程** — 在插件面板中，如果当前视频有对应课程（BV1mJ4m147PG 对应「正当防卫的构成要件」课程），面板应显示课程信息：
   - 课程标题：正当防卫的构成要件 — 罗翔刑法
   - 总步骤数：5
   - 当前步骤：第 1 步「起因条件：必须存在不法侵害」

2. **第 1 步 — 正确回答**：
   - 阅读题目：关于张三误伤物业工人的案例
   - 输入正确答案（包含「不构成正当防卫」「假想防卫」「不存在不法侵害」等关键词）
   - 提交后观察：
     - 是否显示「通过」
     - 是否获得星星（首次通过 = 3 星）
     - 妙喵反馈是否指出关键要点
     - 是否自动跳转到第 2 步

3. **第 1 步 — 语音答题**：
   - 重新进入第 1 步或从第 2 步回到第 1 步
   - 按住 🎤 按钮说出答案：「不构成正当防卫，因为这是假想防卫，没有现实的不法侵害」
   - 松开后自动识别并提交
   - 观察是否能正确评分并给出反馈

4. **第 2 步 — 错误回答再重试**：
   - 故意输入一个错误答案（如只写「正当防卫」）
   - 观察：
     - 是否显示「未通过」
     - 妙喵是否指出漏答的要点
     - 是否显示「⏪ 回退到视频片段」建议
     - 星星是否减少（重试通过 = 2 星或 1 星）

5. **继续完成所有 5 步**：
   - 逐步完成剩余步骤
   - 观察进度条/星星/鱼/成长值的变化

6. **刷新页面后恢复**：
   - 完成部分步骤后，刷新浏览器页面
   - 重新打开插件面板
   - 确认之前的进度（星星数、完成步骤、当前步骤）已恢复

**预期结果**：
- 课程正确加载，步骤信息完整
- 文字答题评分准确（关键词匹配 + LLM 语义判断）
- 语音答题能被识别并正确提交
- 通过/未通过的反馈信息清晰
- 星星、鱼、成长值正确累加
- 进度持久化到 SQLite，刷新后不丢失

---

### T8 — 社区板块

**目标**：验证社区功能基本可用。

**操作步骤**：

1. 访问 http://localhost:3000/community
2. 检查页面是否展示话题列表
3. 如果有「发帖」功能：
   - 输入标题和内容
   - 提交后确认话题出现在列表中
4. 点击某个话题，查看详情和回复
5. 如果有「回复」功能：
   - 输入回复内容
   - 提交后确认回复出现

**预期结果**：
- 社区页面正常加载
- 话题列表有内容
- 发帖/回复功能正常（如果已实现）

---

### T9 — 简历打印页

**目标**：验证简历页面和打印功能。

**操作步骤**：

1. 访问 http://localhost:3000/resume
2. 检查简历内容是否完整（教育背景、项目经历、技能等）
3. 点击页面上的「打印」按钮（如果有）
4. 在打印预览中检查排版是否适合 A4 纸

**预期结果**：
- 简历内容完整、排版整洁
- 打印预览格式正确

---

## 四、异常场景测试

### 4.1 无 LLM 环境

1. 停止后端
2. 在 `.env` 中注释掉所有 API Key
3. 重启后端
4. 在网页中与妙喵对话
5. 预期：妙喵仍能通过本地 FAQ 回答基础问题，复杂问题返回「连不上后端大脑」提示

### 4.2 数据库损坏恢复

1. 停止后端
2. 删除 `data\miaomiao.db` 文件：`Remove-Item data\miaomiao.db`
3. 重启后端
4. 预期：后端自动重建数据库并重新注入种子数据
5. 访问网站，确认数据恢复正常

### 4.3 并发访问

1. 打开 3-5 个浏览器标签页，同时访问首页
2. 在多个标签页中同时与妙喵对话
3. 预期：所有请求正常响应，无 500 错误或数据混乱

### 4.4 限流测试

1. 快速连续发送 25 次聊天请求（< 1 分钟）
2. 预期：第 21 次请求开始返回 429 Too Many Requests
3. 等待 1 分钟后再次发送，应恢复正常

### 4.5 管理后台未授权访问

1. 打开 http://localhost:8000/admin
2. 不输入 Token 直接尝试访问 API
3. 预期：返回 401 Unauthorized

---

## 五、测试数据速查

### 可用课程

| 课程 ID | 标题 | 对应视频 BV 号 | 步骤数 |
|---------|------|---------------|--------|
| `lesson_luoxiang_001` | 正当防卫的构成要件 — 罗翔刑法 | BV1mJ4m147PG | 5 |

### 课程答题参考（第 1 步）

**题目**：张三深夜回家，误伤修下水道的物业工人，是否构成正当防卫？

**正确答案要点**（至少命中 2 个）：
- 不构成正当防卫
- 不存在不法侵害
- 假想防卫
- 没有现实的不法侵害
- 事实认识错误

**错误答案**（命中任一则扣分）：
- 构成正当防卫
- 防卫过当
- 紧急避险

### 常用测试问答

| 用户输入 | 期望匹配方式 |
|----------|-------------|
| `你是谁` | FAQ 精确匹配 |
| `最近在忙什么` | 日记触发词 → 日记查询 |
| `这个视频讲了什么` | 字幕上下文 + LLM |
| `正当防卫的条件` | 字幕搜索 / LLM |
| `怎么联系博主` | FAQ 匹配 |

### 种子数据博主信息

- **Slug**: `ashley`（默认）/ 可通过 `NEXT_PUBLIC_SITE_SLUG` 切换
- **显示名**: 钟笑咪
- **所在地**: 宁波大学阳明学院
- **身份**: AI 应用开发者、校园创业者

---

## 六、常见问题排查

| 问题 | 可能原因 | 解决方法 |
|------|----------|----------|
| 页面空白 | 前端未启动或端口不对 | 确认 `npm run dev` 在运行，访问 3000 端口 |
| API 返回 404 | 后端未启动 | 确认 `python run.py` 在运行 |
| 聊天无回复 | LLM Key 未配置 | 在 `.env` 中设置有效的 API Key |
| 聊天返回 500 | LLM API 调用失败 | 检查 Key 是否有效，网络是否通畅 |
| 插件无反应 | 后端未启动或 URL 配置错误 | 确认后端可访问 `http://8.130.190.169:8000/health`；若用本机后端，修改 `extension/content/*.js` 中的 `API_BASE` 并重新加载插件 |
| 语音输入无反应 | 麦克风权限被拒绝 | 在 B站页面地址栏左侧允许麦克风权限；或检查 Chrome 站点设置 |
| 课程加载失败 | 课程 JSON 文件缺失 | 检查 `server/data/miaomiao/lessons/` 目录是否存在 `lesson_luoxiang_001.json` |
| 管理后台 401 | Token 不正确 | 查看后端启动日志中的随机 Token |
| 数据库锁定 | 多进程同时写入 | 关闭其他使用同一 DB 的进程 |
